import { CourseDirection, CourseFormat, EducationType, Location } from './course';

export type Dictionaries = {
  directions: CourseDirection[];
  formats: CourseFormat[];
  educationTypes: EducationType[];
  locations: Location[];
};
